apply plugin: 'java-gradle-plugin'

gradlePlugin {
    plugins {
        simplePlugin {
            id = 'cucumber.testit.exporter'
            implementationClass = 'cucumber.testit.exporter.gradle.plugin.CucumberTestItExporterPlugin'
        }
    }
}

dependencies {
    implementation project(':exporter')
}